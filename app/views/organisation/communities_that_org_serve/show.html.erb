<%=
  render partial: "partials/page_title",
         locals: {
             model_object: @organisation,
             page_title: t('communities.page_title')
         }
%>

<%= content_for :secondarycontent do %>
  <%= 
    render partial: "partials/summary/summary_link", locals: {
      summary_link: organisation_summary_path
    } 
  %>
<% end %>

<%= 
  render partial: "partials/summary_errors",
  locals: {
    form_object: @organisation,
    first_form_element: :organisation_communities_that_org_serve_path
  } if @organisation.errors.any? 
%>

<h1 class="govuk-heading-xl govuk-!-margin-bottom-3">

  <span class="govuk-caption-xl">
    <%= t('communities.page_heading_caption') %>
  </span>

  <%= t('communities.page_heading') %>

</h1>

<div class= "govuk-!-margin-bottom-3">

  <span class="govuk-hint ">

      <%= t('communities.page_hint') %>
      
  </span>
  
</div>
  

<%= 
  form_with model: @organisation,
  url: organisation_communities_that_org_serve_path,
  method: :put,
  local: true do |f| 
%>

  <%=
    render(
      partial: 'partials/organisation/communities_that_org_serve_question',
      locals: {
        model_object: @organisation,
        model_object_name: 'organisation',
        form_object: f
      }
    )
  %>

   <%= render(
         ButtonComponent.new(
         element: 'button',
         is_start_button: false,
         text: t('buttons.labels.default')
         )
    )%>
  
<% end %>
